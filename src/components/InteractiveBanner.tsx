import * as React from "react";import 'bootstrap/dist/js/bootstrap.bundle.min';import { useNavigate } from 'react-router-dom';import HeadingH2 from "./text/HeadingH2";import NavigateButton from "./button/NavigateButton";interface InteractiveBannerProps {	title: string;	buttonText: string;	gradientStart: string;	gradientEnd: string;	redirection: string;}const InteractiveBanner: React.FC<InteractiveBannerProps> = ({ title, buttonText, gradientStart, gradientEnd, redirection}) => {	const navigate = useNavigate();	const gradientStyle = {		minHeight: '50vh',		background: `linear-gradient(to right, ${gradientStart}, ${gradientEnd})`	};		const handleDonateClick = () => {		navigate(redirection);	};		return (		<div className="banner py-5 d-flex align-items-center shadow-lg bg-darker" style={gradientStyle}>			<div className="container">				<div className="row justify-content-center">					<div className="col-md-8 text-center">						<HeadingH2 text={title} />						<NavigateButton onClick={handleDonateClick} text={buttonText} />					</div>				</div>			</div>		</div>	);};export default InteractiveBanner;