import React from 'react';interface ReservedButtonProps {	status: string;	onClick: () => void;}const ReservedButton: React.FC<ReservedButtonProps> = ({ status, onClick }) => {	return (		<button			className={`btn ${status === "reserved" ? "btn-secondary" : "btn-primary"} btn-sm`}			onClick={onClick}			disabled={status === "reserved"}		>			{status === "reserved" ? "Reserved" : "Join"}		</button>	);};export default ReservedButton;