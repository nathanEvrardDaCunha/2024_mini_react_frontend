import React from 'react';import LabelForm from "./LabelForm";import Input from "./Input";interface InputFieldProps {	label: string;	type: string;	name: string;	value: string | number;	onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;	min?: number;	required?: boolean;}const InputField: React.FC<InputFieldProps> = ({   label,   type,   name,   value,   onChange,   min,   required,}) => (	<div className="mb-4">		<LabelForm htmlFor={name} text={label} />		<Input			type={type}			name={name}			value={value}			onChange={onChange}			min={min}			required={required}		/>	</div>);export default InputField;