import { useState } from "react";import axios from "axios";import Cookies from "js-cookie";import InputField from "./InputField";import SubmitButton from "./SubmitButton";interface FormData {	email: string;	password: string;}const LoginForm: React.FC = () => {	const [formData, setFormData] = useState<FormData>({		email: "",		password: "",	});		const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {		setFormData({ ...formData, [e.target.name]: e.target.value });	};		const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {		e.preventDefault();		try {			const response = await axios.post("http://localhost:3000/employee/login", formData);			const { userId } = response.data;			Cookies.set("userId", userId);			//TODO : Make the user understand this worked visually		} catch (error) {			//TODO : Make the user understand this didn't worked visually		}	};		return (		<div className="row justify-content-center align-items-center min-vh-100 mx-0">			<div className="col-md-8 col-lg-6 px-5">				<h1 className="text-center mb-4">Login</h1>				<form onSubmit={handleSubmit}>					<InputField						label="Email"						type="email"						name="email"						value={formData.email}						onChange={handleChange}					/>					<InputField						label="Password"						type="password"						name="password"						value={formData.password}						onChange={handleChange}					/>					<SubmitButton  text={"Login"}/>				</form>			</div>		</div>	);};export default LoginForm;