import React, { useState } from 'react';import JoinSessionModal from '../JoinSessionModal';import TableHeader from "./TableHeader";import TableBody from "./TableBody";import TableCell from "./TableCell";import ReservedButton from "../button/ReservedButton";interface Session {	id: string;	theme: string;	startDate: string;	endDate: string;	price: number;	minimalClient: number;	employeeId: string;	status: string;}interface SessionUserTableProps {	sessions: Session[];	onJoin: (sessionId: string, email: string, numberOfPeople: number) => Promise<void>;	onStatusChange: (sessionId: string) => Promise<void>;}const SessionUserTable: React.FC<SessionUserTableProps> = ({ sessions, onJoin, onStatusChange }) => {	const [showModal, setShowModal] = useState(false);	const [selectedSessionId, setSelectedSessionId] = useState('');		const handleJoinClick = (sessionId: string) => {		setSelectedSessionId(sessionId);		setShowModal(true);	};		const handleJoinSession = async (email: string, numberOfPeople: number) => {		await onJoin(selectedSessionId, email, numberOfPeople);		await onStatusChange(selectedSessionId);		setShowModal(false);	};		if (sessions.length === 0) {		return <p className="text-center">No sessions found.</p>;	}		const formatTime = (dateString: string) => {		const date = new Date(dateString);		return date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });	};		const selectedSession = sessions.find((session) => session.id === selectedSessionId);		return (		<div className="table-responsive">			<table className="table table-dark table-striped">				<thead>				<tr>					<TableHeader text="Theme"/>					<TableHeader text="Start Time"/>					<TableHeader text="End Time"/>					<TableHeader text="Price"/>					<TableHeader text="Minimal Clients"/>					<TableHeader text="Actions"/>				</tr>				</thead>				<TableBody>					{sessions.map((session) => (						<tr key={session.id}>							<TableCell>{session.theme}</TableCell>							<TableCell>{formatTime(session.startDate)}</TableCell>							<TableCell>{formatTime(session.endDate)}</TableCell>							<TableCell>{session.price}</TableCell>							<TableCell>{session.minimalClient}</TableCell>							<TableCell>								<ReservedButton									status={session.status}									onClick={() => handleJoinClick(session.id)}								/>							</TableCell>						</tr>					))}				</TableBody>			</table>			{selectedSession && (				<JoinSessionModal					show={showModal}					onClose={() => setShowModal(false)}					onJoin={handleJoinSession}					minimalClient={selectedSession.minimalClient}				/>			)}		</div>	);};export default SessionUserTable;